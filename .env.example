# JuiceBox Example Environment Variables
# Copy this file to .env and edit as needed

# Maximum file size per upload (e.g. 500MB, 1GB, 104857600 for 100MB)
MAX_FILE_SIZE=500MB

# App environment (set to 'production' for prod)
APP_ENV=development

# Secret key for hashing IP addresses (min 16 bytes). Use base64/hex or plain string.
# Example: openssl rand -hex 32
IP_HASH_SECRET=

# Optional overrides for where Juicebox reads/writes data. All paths accept
# absolute values; relative values are resolved against JUICEBOX_STORAGE_ROOT
# when that variable is set. JUICEBOX_CHUNK_DIR defaults under the data dir if
# you provide a relative value.
JUICEBOX_STORAGE_ROOT=
JUICEBOX_PUBLIC_DIR=
JUICEBOX_DATA_DIR=
JUICEBOX_UPLOAD_DIR=
JUICEBOX_CHUNK_DIR=
# Canonical host name used for generated links when APP_ENV=production (defaults to box.juicey.dev)
JUICEBOX_PROD_HOST=

# Mailgun (optional, for report emails)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
REPORT_EMAIL_TO=
REPORT_EMAIL_FROM=

# Optional Cloudflare purge configuration - when present the server will
# attempt to purge the file URL from Cloudflare after a successful delete.
# If these are not set the server will no-op the purge (good for local/dev).
# Example: obtain an API token scoped to Zone.Cache Purge for your zone.
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=

# Here goes sentry dsn if you want to enable Sentry error reporting
SENTRY_DSN=
# example: https://<key>@o<org>.ingest.sentry.io/<project>
# if it is not set, Sentry will be disabled, and errors will be logged to console only

# Optional Sentry environment label (defaults to APP_ENV)
SENTRY_ENV=

# Optional release identifier exposed to Sentry Releases
SENTRY_RELEASE=

# Tracing sample rate for performance spans (0.0 - 1.0).
# Defaults to 0.1 in production and 0 elsewhere when unset.
SENTRY_TRACES_SAMPLE_RATE=

# Set to 1/true to crash on startup and verify the Sentry pipeline end-to-end
SENTRY_VERIFY_PANIC=0
